# Migration Tests

## Files

- `README.md`: this migration test folder guide.
- `billing-exact-once.test.mjs`: verifies authoritative paid-through plus non-`FREE` schema/API/worker threading, admin exact-session Serializable grant ownership, immutable `balanceAfter` storage/replay with retained nullable-writer rollout compatibility, attributed audit replay, and fail-closed legacy settlement handling.
- `bounded-child-process.test.mjs`: proves migration subprocess deadlines terminate a TERM-ignoring descendant tree before it can perform delayed output.
- `availability-import-state-machine.test.mjs`: verifies leased publication recovery, fresh no-op and populated replay semantics for the availability pre-migration, nullable bounded encrypted source expansion, separate public document and active-account identity checks, API/worker secret wiring, forced tenant isolation, deletion cancellation/refund/source-erasure barriers, quote-agnostic lifecycle contracts, final-handoff lock order, and 24-hour terminal retention.
- `audit-log-retention.test.mjs`: verifies deleted-user and 30-day tenant audit pseudonymization preserve immutable event evidence while seven-year deletion remains separately restricted to the platform-admin security-definer owner function.
- `credit-refund-debt.test.mjs`: verifies the additive debt schema and exact zero backfill, nonnegative constraints, immutable debt ledger fields, retained old-writer compatibility that fails closed while debt exists, the cross-runtime invoker-rights debt-first settlement function, zero-debt billable debit policy, and delegation of every runtime positive-value writer to a canonical debt-first owner.
- `credit-provenance.test.mjs`: verifies legacy tenant imports create no implicit usage-credit grant and the schedule refund migration locks settlement, rejects debit provenance drift, derives refunds from exact ledger debits, and increments wallets only for newly inserted refunds.
- `data-target-guard.test.mjs`: verifies destructive seed, import, migration, and VM107 restore guards fail before Prisma or host mutation.
- `database-role-isolation.test.mjs`: verifies runtime services use a restricted Postgres role while migrations retain the owner URL, and checks idempotent role attributes, grants, and default privileges.
- `email-delivery-feedback.test.mjs`: verifies provider feedback ordering, durable suppression fields, active-recipient lookup, email-change/deletion erasure, and schema/migration alignment.
- `legacy-parity-inventory.test.mjs`: validates that the legacy PHP source, legacy tests, TypeScript platform surfaces, Prisma schema, and migration documentation cover the required parity workflows.
- `legacy-unbacked-credit-cleanup.integration.test.mjs`: disposable PostgreSQL proof for exact fixed-import and reconciliation provenance, obsolete-marker immunity, repeatable later-row reconciliation, post-marker fixed imports, preservation of legitimate and mixed ledger-backed balances, a barrier-controlled actual `MeteringService` grant versus cleanup race without `40P01`, fail-closed consumed/zero-history ambiguity, transaction rollback, and replay.
- `legacy-unbacked-credit-cleanup.test.mjs`: static importer and forward-migration contracts for atomic per-tenant zero-credit provenance, exact table/row locks, repeatable known-grant-only reconciliation evidence, no ledger mutation, malformed/ambiguous history rejection, no global completion marker, and exact digest-bound raw-policy approval.
- `location-timezone-default.test.mjs`: verifies the Prisma model has no silent timezone default and the replay-safe forward migration drops only the database default without rewriting existing locations.
- `notification-outbox-reliability.test.mjs`: verifies tenant-RLS outbox state, dedupe and lease indexes, commit-before-delivery ordering, skip-locked crash recovery, deterministic per-publication notification identity, corrected-schedule republish delivery, and terminal visibility.
- `notification-terminal-payload-erasure.test.mjs`: verifies processor and database-trigger content plus last-error erasure when notification delivery reaches a terminal state.
- `onboarding-signup-attempt-retention.test.mjs`: verifies stable onboarding identifier hashes have a fixed 24-hour database retention boundary, capability-gated cleanup, scheduled endpoint integration, and documentation.
- `password-reset-token-retention.test.mjs`: verifies expired or consumed password-reset token hashes have a fixed 24-hour grace, bounded capability-gated cleanup, scheduled reporting, and active-token exclusion.
- `payroll-controls.test.mjs`: verifies the exact 12-model payroll schema, enums including ADOPT, forced tenant RLS and composite foreign keys, forward-only policy/period/card/approval controls, signed amendments, immutable lock/export/reconciliation evidence, and legal-hold-aware two-stage purge gates.
- `subscription-credit-invariants.test.mjs`: verifies zero-credit signup and provisioning, single-owner idempotent grants, positive wallet-only debits, and database rejection of negative wallets or plan-owned credit quotas.
- `platform-admin-audit-attribution.test.mjs`: verifies target-tenant audit semantics, non-relational platform actor identity, immutable actor columns, and unchanged audit RLS.
- `platform-admin-rls.test.mjs`: evaluates final migration ordering and RLS policy contracts so capability-authenticated platform operations can read cross-tenant workspace, RBAC, and audit rows while tenant context remains isolated.
- `prisma-migration-contract.test.mjs`: verifies raw SQL migrations target Prisma's quoted identifiers, void advisory-lock functions use parameterized execution instead of row deserialization, stage existing solve-job request hashes before required-column schema push, keep RBAC replay least-privilege and assignment-safe, cover schema changes such as `TimeCard`, first-location request identity, and persisted scheduler inputs, and retain public-SaaS database integrity contracts.
- `raw-migration-rollback-policy.test.mjs`: regenerates the candidate-only raw/pre migration policy from its immutable historical baseline, requires exact non-payroll path/digest coverage, locks the legacy cleanup digest, exercises the governance migration's mandatory old-release proof through the rollback verifier, and proves historical drift, missing Python, rejected SQL, stale semantics, and malformed manual approval fields fail closed.
- `raw-migration-ledger.test.mjs`: verifies exact receipt skips and drift rejection, atomic SQL/receipt commits, authenticated historical baselining, production bootstrap refusal, migration transaction-boundary rejection, and both committed and absent unknown-COMMIT recovery paths.
- `saas-database-performance.test.mjs`: verifies tenant-leading hot-path indexes, deterministic list/timeline ordering, and batched user-directory role loading.
- `session-retention.test.mjs`: verifies platform-admin-only bounded deletion of sessions expired beyond 24 hours or revoked beyond 30 days, replay-row cascade cleanup, scheduler wiring, and exclusion of active credentials.
- `scheduling-public-launch-integrity.test.mjs`: verifies fresh/populated-safe API v2 public identifier expansion, shift/schedule window triggers, durable auto-schedule request uniqueness, terminal solve queue/lease/error/claim erasure, and exact-debit-derived worker/publication refund settlement.
- `shift-overlap-constraint.test.mjs`: verifies assigned-shift no-overlap, schedule window/overlap, shift tenant/location, and break window database constraints plus their schema prerequisites.
- `staff-availability-overnight.test.mjs`: verifies overnight endpoints plus forced tenant RLS, tenant policies, and composite staff/location integrity for persisted scheduling profiles.
- `staff-invitation-outbox.integration.test.mjs`: disposable PostgreSQL proof for populated-schema tenant-first user identity staging and cross-tenant invitation foreign-key rejection.
- `staff-invitation-outbox.test.mjs`: verifies fresh-safe and populated-replay pre-schema creation of Prisma's exact tenant-first User identity index without accepting data loss, invitation status/table shape, tenant-consistent ownership, forced RLS, envelope/state constraints, terminal erasure, user/tenant lifecycle cancellation, bounded retention authority, and due/dead-letter indexes.
- `stripe-usage-logical-identity.test.mjs`: verifies deterministic Stripe usage snapshot dedupe and immutable tenant, metric, and period uniqueness.
- `tenant-account-lifecycle-rbac.test.mjs`: verifies fresh and upgrade ordering, duplicate-safe lifecycle permission reconciliation, and active system Admin-only grants without custom-role privilege expansion.
- `tenant-data-governance-p1.test.mjs`: verifies null-safe platform capability proof, canonical legal-hold/user-deletion locking, fail-closed security-definer retention ownership, Prisma-owned export cleanup fields/index, durable cleanup and purge guards, complete fsync/rename publication ordering, snapshot watermark capture, attributed deletion audits, and pre-provider cancellation/deletion-barrier contracts.
- `tenant-deletion-billing-reconciliation.test.mjs`: verifies Prisma and forward-migration ownership of durable deletion-billing attempts, paired leases and fencing, barrier backfill, forced tenant RLS, executable committed-claim/provider/exact-owner-token finalization order, fair skip-locked ordering, exponential backoff, and autonomous reconciler wiring.
- `terminal-encrypted-payload-erasure.test.mjs`: verifies delivered/dead-lettered password-reset token hashes are pseudonymized and password-reset/webhook ciphertext, key references, and diagnostics are irreversibly erased.
- `user-deletion-anonymization.test.mjs`: verifies database-enforced direct PII/credential scrubbing, stable session tombstones, signup/notification cleanup, audit pseudonymization, deleted-user session-revival rejection, and historical reconciliation.
- `user-suspension-lifecycle.test.mjs`: verifies reversible suspension remains separate from deletion across schema, database triggers, and the canonical RbacService tenant/user/session/assignment/role/permission lock owner and call order, plus exact actor-session/target isolation, authentication/capacity filters, and platform user status controls.
- `webhook-reliability.test.mjs`: verifies fresh migration helper ordering, nullable terminal retry timestamps, durable-before-network first-attempt crash recovery, ACTIVE subscription plus tenant-scoped credit-reservation delivery eligibility, transaction-free attempt-fenced sends, deterministic terminal refunds, nonterminal pause recovery, PURGED-only lifecycle terminalization, and aggregate remaining-time webhook replay shutdown draining.
- `webhook-key-rollback-readiness.test.mjs`: verifies retained-release key overlap, candidate activation rejection without preloaded rollback keys, and post-schema-only webhook envelope rotation.
- `webhook-secret-preflight.test.mjs`: verifies v2 encryption output and legacy v1 envelopes pass structural preflight while plaintext or malformed values fail without destructive SQL.

## Purpose

These tests protect the migration contract between the live PHP behavior and the TypeScript SaaS platform.

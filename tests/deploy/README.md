# Deploy Tests

## Files

- `README.md`: this deploy test folder guide.
- `active-release-runtime.test.mjs`: verifies staging cleanup cannot invalidate the durable current release; backup/PITR systemd jobs execute the exact retained release and Compose image selected by root-owned `IMAGE_TAG`; and probe, marker, and retention jobs intentionally follow `/opt/lunchlineup/current`.
- `auth-secret-rotation.test.mjs`: verifies managed current/previous MFA envelopes, legacy v1 and plaintext overlap reads, strict key validation, serializable all-row re-encryption, current-only removal proof, fail-closed rollback, and read-only help.
- `backup-scheduler.test.mjs`: verifies the ops-profile backup job, full canonical systemd unit identifiers mapped internally to Compose services, exact retained-candidate execution with a real wrapper completion record and fresh InvocationID/path/source/image-digest binding, deterministic retag resistance through a digest-rewritten Compose snapshot, the real transport remote-materialization script handing the canonical retained path to readiness without appending another release segment, per-job atomic metric freshness across a clock-advanced six-hour PITR simulation with stale pre-run rejection, coherent 2-hour/6-hour client budgets, timeout reconciliation with no survivor, exact service/timer restoration, encrypted key secret, and operator docs.
- `backup-restore-dr.test.mjs`: verifies immutable S3 logical-backup proof and lifecycle expiry ownership, the shared TERM-then-KILL provider-command boundary and byte caps, DR fetch/readback process-group TERM-then-KILL cleanup against a TERM-ignoring delayed-rewrite child, signed externally fetched recovery-adapter and exact execution provenance, deterministic replacement of both adapter executables, their attestation/bundle, and provider readback while only private stable snapshots reach Cosign/hash/execution/parse, password-free Docker argv via a protected env file, one private backup snapshot surviving source replacement, 600-second aggregate restore/DR caps, one absolute container-to-proof DR deadline with immutable-ID timeout cleanup and no success evidence, cleanup race rejection, private snapshot-only production restore evidence, timeout exit `70` with bounded reconciliation and child termination, target/system-identifier pinning, and fail-fast unsafe-input handling.
- `build-launch-proof-manifest.test.mjs`: verifies deterministic secret-free assembly of all six required launch-evidence kinds, input-relative CLI evidence paths with reproducible JSON output, self-contained provider-authenticated DR provenance, nested DAST/load raw-result digest claims, and rejection of missing, empty, stale, future, detached, duplicate, mutable, non-public, non-HTTPS, or secrets-like evidence.
- `container-runtime-hardening.test.mjs`: verifies every production service uses a read-only root and no-new-privileges, stateless services drop all capabilities, writable paths are explicit and bounded, CPU/memory/PID/file-descriptor/log retention ceilings are present, release application images declare non-root runtime users, and hostile PDF parsing runs in a secret-free no-network one-document container boundary.
- `ci-old-release-clone-driver.test.mjs`: verifies emergency rollback and production deploy use pinned Node/Python runtimes plus the isolated mode-0700 secret clone driver, exact provision/harness mappings, bounded TERM-then-KILL execution, and always-run bounded destroy cleanup.
- `deploy-alert-gate.test.mjs`: verifies fresh, exact loopback Prometheus and Alertmanager responses; complete LunchLineup alert-group coverage; fail-closed critical-alert handling; bounded response age; and alert verification before deployment proof or pointer promotion.
- `deployed-release-inputs.test.mjs`: verifies smoke and rollback publication consume the exact deploy-job secret descriptor and proof bytes, reject digest drift, require a mode-`0600` retained-proof URI file, argv-spy the real CI binding arguments, reject raw/base64 URI argv, and never persist runtime secret bytes.
- `deploy-source.test.mjs`: verifies deploy-source scripts exist and fixture-tests clean Git state, upstream push proof, and `DEPLOYED_GIT_SHA` checks where local shell runtimes are available.
- `deploy-vm217-remote.test.mjs`: verifies authenticated-registry-current versus live-SHA pre-mutation blocking, SHA-bound public health and Next.js gates, request/loop deadlines against a nonresponding peer, required-service checks, bounded fresh Prometheus/loopback Alertmanager verification before proof and pointer promotion, durable pre-mutation rollback arming, signed compatibility-proof propagation into raw-migration verification, and trigger/RLS default rejection.
- `deploy-vm217-transport.test.mjs`: fixture-tests pinned-host runner transport, aggregate mutation-budget start before the first remote staging/upload, pre-upload exhaustion with zero SCP mutation plus bounded reconciliation/cleanup, manifest-exact SHA-addressed retained materialization, real-Compose-derived service/image identity, exact runtime/backup/timer/pointer/traffic readback after unknown state, protected URI argv exclusion, local/remote hash equality, checked-in entrypoint execution, and secret-safe bounded staging cleanup without network access.
- `emergency-rollback-protection.test.mjs`: parses the CI workflow and verifies mutually exclusive main-only dispatch, production-environment binding, pinned Node/Python setup, the exact 6,000-second outer equality including both `npm ci`, Python install, provider/harness inspection, clone teardown and proof-finalization bounds, absolute pre-mutation cutoff enforcement, meaningful post-mutation reserve, exact manifest/runtime mappings, signed compatibility proof, authenticated registry repoint/readback, and required external reviewer/wait-gate documentation.
- `external-health-release.test.mjs`: verifies deadline-aborted nonresponding peers, canonical public HTML and release SHA, public and Cloudflare Access-authenticated probes, paired-credential fail-fast behavior, and secret-free proof output.
- `fake-cosign-release-report.mjs`: deterministic test-only Cosign substitute for release-report blob signatures and image-attestation subject, signer, issuer, and predicate failure fixtures.
- `initial-vm217-cutover.test.mjs`: fixture-tests the one-time legacy PHP cutover's independently bounded external snapshot, proof-fetch, rollback, and pinned VM217 transport operations; strict proof schema/freshness; full exact-state reconciliation after every rollback and candidate success; false/no-op success exit `70`; protected URI argv exclusion; pre-network refusal; and rollback on delegated deploy failure or timeout.
- `launch-proof-evidence.test.mjs`: verifies signed external DR-adapter provenance claims, signed exact DR/PITR execution-provider-readback-target-outcome-cleanup bindings, exact DR container-absence evidence, forged self-consistent cleanup evidence rejection, fresh provider-authenticated versions, retained alert evidence, candidate DAST/load bundles, exact release/image binding, and digest/byte tamper rejection.
- `load-smoke-policy.test.mjs`: verifies the launch load gate uses executable 200-only assertions, zero failed-expectation tolerance, complete request/response accounting, a strict p99 budget, read-only source plus bounded writable output mounts, the immutable Artillery runner, and bounded authenticated availability-import proof.
- `observability-configs.test.mjs`: verifies private Loki log shipping and Tempo trace paths, deploy gating, unique Grafana panel/query references, isolated PDF parser runtime-loss coverage, worker-owned ready/retry/DLQ solver telemetry with single-poison alert fixtures, recent recoverable delivery dead-letter alerts, execution-only retention missing/stale alerts plus dashboard panels, and the structured Node verifier's digest-pinned real-container Caddy, Prometheus config/rules, both promtool fixtures, and Alertmanager validation when Docker is available.
- `old-release-clone-cleanup.test.mjs`: fixture-tests first-attempt clone destroy failure, driver/env preservation for `always()` retry, successful second destroy, and proof that no compatibility clone survivor remains.
- `old-release-compatibility-ci-contract.test.mjs`: executable Planck handoff that parses both real CI callers for the complete clone-network and immutable provider runtime/digest/image options, checks trusted provider-owned tool paths, and enforces production-runtime argv/mount exclusion without editing the workflow.
- `old-release-compatibility-harness.test.mjs`: executable-tests GitHub-run-owned internal clone-provider identity, clone-only credentials, immutable runtime/image and contract digest binding, provider-local npm/Python preparation, exact hostile npm postinstall and Python build-backend probes for production-runtime/clone-command absence, root-owned schema diff/catalog evidence, production path/secret argv exclusion plus metadata preservation, timeout, arbitrary text, detached proof, mutation, survivor, adapter, evidence-tamper, and mutable-URI rejection before signing.
- `postgres-pitr.test.mjs`: executable-tests disabled archive failure and non-PITR `archive_mode=off`, private request-scoped WAL/provider credential isolation, container-owned provider processes, source-adjacent exact readback, hardened shared backup image tooling, rendered forwarding plus fake-provider use of all four restore version IDs, conditional immutable upload conflict/unknown state, provider authorization simulation for both read-only identities with overprivilege rejection, lifecycle proof fixtures, protected provider argv/config behavior, writer deletion denial, COMPLIANCE Object Lock, bounded expiry, isolated restore guards, shell syntax, timer wiring, and the recovery runbook.
- `production-compose.test.mjs`: verifies Compose, managed MFA key injection, proxy, isolated Alertmanager paging egress, RabbitMQ named-volume persistence, observability, webhook replay worker wiring, shared invitation attempt/config propagation and required API/worker `APP_ORIGIN`, fail-closed invitation sweep health, migration data-target and isolated database-role propagation, immutable release gates, canonical production-web smoke before success publication, generated smoke env, backup/restore safety, all four release-verifier Compose scope flags, and a fail-closed direct-command allowlist across fenced/inline runbook Markdown that rejects variables, functions, aliases, substitutions, helpers, and multiline evasions.
- `production-deploy-deadlines.test.mjs`: parses the normal deploy job and machine-checks the pre-checkout absolute deadline origin, elapsed preflight debit, compatibility/mutation/reconciliation/cleanup reserves, direct checked-in pinned-host transport invocation, and an actual nearly exhausted owner fixture that hard-kills a TERM-ignoring provider while clone destruction still runs.
- `production-image-inventory.test.mjs`: verifies YAML-parsed quoted/underscore/inline/indented/inherited service definitions, complete first- and third-party production Compose coverage, shared-image deduplication, CI matrix ownership, and fail-closed missing, duplicate, or mutable images.
- `production-launch-proof.test.mjs`: verifies snapshot-only production launch-proof inputs, placeholder rejection, CI proof-secret gate, exact immutable DR container-ID cleanup requirements, recovery-bearer exclusion from `/proc` provider argv and child environment through a mode-0600 curl config, and the documented template verification path.
- `publish-release-evidence.test.mjs`: verifies the immutable evidence release contains the complete Compose-derived signed image-report inventory plus candidate canonical/raw assets and rejects raw, release, or remote-asset digest drift.
- `release-bundle-registry.test.mjs`: verifies keyless signed secret-free publication, protected bootstrap, unconditional all-identity exact-prefix delete denial variants, active-safe/obsolete-bounded S3 lifecycle and Object Lock preflight, exact immutable readback, provider-conditional pointer/signature writes, pre-pointer failure reconciliation, authenticated split-pair repair, idempotent target handling, competing-pointer and protection-drift refusal, tamper refusal, and rollback baseline wiring. The 82-probe conditional provider scenario executes on Linux CI with a 120-second test timeout; Windows uses a fail-fast source-contract assertion because nested Git-Bash-to-Node provider ownership exceeds the local 120-second lane bound.
- `signed-release-authenticity.test.mjs`: deterministically atomically replaces release state, index, and signature paths after Cosign starts and proves hashing, parsing, and both signature checks consume only the original private snapshots.
- `release-pipeline-hardening.test.mjs`: verifies the version-2 retained archive is canonical and secret-free, includes package/lock/workspace manifests, requirements, Prisma schema/migrations, integration owners, infrastructure/systemd units and every release Dockerfile, excludes generated/secret artifacts, parses CI YAML to prove mandatory integration gates pull requests plus develop/main pushes while image publication remains main-only, and preserves source-bound DAST/load evidence gates.
- `release-report-provenance.test.mjs`: verifies SBOM and Trivy reports are keyless-signed, exact-image-attested, retained for the public Actions maximum, and archived in a source-bound immutable GitHub release.
- `rollback-release-state.test.mjs`: verifies signed-index and signed-bundle authentication before materialization, builds and materializes real old/current version-2 archives, runs exact compatibility preflight from both roots, exact-compares retained systemd units, rejects one-byte drift plus missing/unmanifested archive bytes, preserves one secret-free baseline for centralized rollback, and failure-injects every post-arm outcome.
- `rollback-vm217-transport.test.mjs`: fixture-tests pinned-host manual rollback transport, absolute cutoff plus noninteractive deadlines, retained-input and signed compatibility proof checks, protected URI argv exclusion, immutable SHA-addressed activation, failure after each pointer commit, hanging activation plus TERM reconciliation-before-cleanup with no trap recursion, recovered success only after exact runtime/backup/timer/service/health/traffic reconciliation, candidate-owned state rejection, fixed unknown-state diagnostics, and bounded cleanup without network access.
- `runtime-env-durability.test.mjs`: fixture-tests atomic release-SHA/digest-bound runtime persistence, root-only transport, service-group durable modes, exact Compose and backup/PITR binding, successful promotion, failure injection after runtime and release pointer commits, failed-deploy restoration, retained rollback restoration, unsafe-mode rejection, digest drift, and redacted logs.
- `runtime-permission-access.fixture.sh`: Linux root fixture that applies real ownership and modes, then drops to a non-root UID/GID to prove runtime-env, Compose, probe, executable, and active-marker access across rollback.
- `runtime-permission-access.test.mjs`: runs the real Linux permission fixture through WSL or a native root test runtime and verifies its output remains secret-free.
- `sbom-release-reports.test.mjs`: verifies every Compose-derived digest-pinned production image has a nonempty Syft SPDX report with trusted signed evidence, requiring exact OCI digest attestations for writable first-party registries while retaining signed digest binding for third-party images.
- `retention-scheduler.test.mjs`: verifies separate confirmed 30-day application-data execution and seven-year retained-record dry-run systemd schedules, including independent alert ownership, token, proof, metrics, locks, timers, docs, and node-exporter textfile collection.
- `password-reset-token-retention.test.mjs`: verifies the daily retention scheduler records bounded password-reset token dry-run proof and stage-labeled Prometheus metrics.
- `tenant-export-durability.test.mjs`: verifies durable export schema/migration, shared API artifact storage, fail-closed runtime configuration, secure permissions, scheduled expiry, and runbook inventory.
- `stripe-meter-config.test.mjs`: fixture-tests authoritative Stripe meter identity and payload mappings, attached live metered plan prices, exact production webhook URL and required event subscriptions, `last` aggregation, launch-proof binding, and workflow ordering without network access.
- `telemetry-pipeline.test.mjs`: verifies internal-only OTLP collection, buffered Tempo export, read-only socket-free Docker log shipping, and worker-to-engine trace propagation.
- `tenant-deletion-billing-observability.test.mjs`: verifies deletion-specific reconciliation metrics, sustained-backlog and stale-sweep Prometheus alerts, dashboard panels, and runbook ownership without colliding with cancellation metrics.
- `term-ignoring-rollback-command.fixture.sh`: validator-passing protected rollback command fixture that ignores TERM, proving rollback jobs must never execute approved arbitrary shell text outside the checked-in deadline-owning transport.

## Purpose

These tests keep server deploys tied to GitHub state, built Git-SHA image artifacts including the required backup image, a Compose-derived SBOM/Trivy inventory for every first- and third-party runtime image, digest-pinned base and third-party service images, isolated Compose project names, structured observability config validation with an optional real-tool validation contract, webhook replay worker startup, observable health surfaces, fail-fast runtime secrets, checked-in alert runbooks, scheduled encrypted offsite backups, backup freshness telemetry, retained-record purge telemetry, auth secret rotation, production runtime-env validation, retained launch-proof evidence, and machine-checkable restore proof instead of direct VM edits. CI DAST and load smoke must verify `release-manifest.json`, pull digest-pinned release images, and run Compose with `--no-build --pull never`; otherwise the smoke jobs can pass against source rebuilt or mutable-tag-pulled inside the runner instead of the artifact being deployed. Launch-proof tests also require command, exit-code, checksum, size, timestamp, source-SHA, and unique-URI metadata so a bare URL cannot satisfy production readiness.

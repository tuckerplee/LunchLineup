# Worker Tests

Unit tests for queue-boundary validation and worker job handling.

## Files

- `README.md`: this folder guide.
- `test_availability_import.py`: public document-identity/server-only account-binding separation, FREE/missing/expired/future authoritative paid-through claim and paid-to-FREE commit barriers independent of credits, immutable debit/refund `balanceAfter` provenance, encrypted restart recovery after local loss, corruption retry classification, verified local fallback, key isolation, terminal source erasure, missing/mismatched/duplicate-debit rejection, ambiguous candidate-token propagation, deterministic rejected-claim and paused-tokenless-retry/live-replacement lease races, concurrent exactly-once refund and wallet settlement, 24-hour retention, parser timeout/reaping, terminal replay, and result bounds.
- `test_billing_usage.py`: durable Stripe usage claim, retry, fresh resubmission identity, immutable logical snapshot upsert, final-attempt crash recovery, last-value-gated dual-identity operator replay, periodic snapshot freshness, current-snapshot exclusion, and over-batch sweep progression coverage, including secret-bearing retry and terminal diagnostic redaction.
- `test_password_reset_email.py`: encrypted reset-envelope compatibility, provider success, suspended/deleted user and non-active tenant rejection at claim and final handoff, suppression rejection, lease-loss safety, transient delivery retry, bounded provider-outage accounting including repeated terminal 401/403 failures after diagnostic erasure, terminal encrypted-envelope erasure, retry-envelope preservation, cycle-level sweep recovery, and liveness/readiness shutdown coverage, including secret-bearing durable failure-code redaction.
- `test_staff_invitation_outbox.py`: disabled-by-default and production-required canonical enablement, bounded schema/config relationships, canonical production `APP_ORIGIN` validation and credential-free login links, a fixed cross-language versioned API AAD/ciphertext fixture plus tamper rejection, platform-capable global `SKIP LOCKED` claims, lease recovery and configured-limit terminalization, final lifecycle cancellation and suppression, stable Resend idempotency after response loss, distinct provider identity after reactivation or reissue, jittered retry/dead-letter behavior, terminal-trigger compatibility, bounded diagnostics, sweep readiness/systemic-failure shutdown behavior, and recipient/secret redaction.
- `test_pdf_parser.py`: parser coverage proving availability imports normalize and hash visible employee codes and email identifiers without returning them, use extracted PDF text, fail closed for invalid uploads, transfer only bounded bytes through the client, exclude production secrets from the parser child, and reject unsafe IPC paths and payloads.
- `test_parser_health.py`: isolated-parser worker-side readiness probe, sanitized child environment, timeout failure, and Prometheus metric coverage.
- `test_worker.py`: stdlib `unittest` coverage for worker validation, production fail-closed broker and isolated-parser dependency monitoring, deterministic poison routing with DLQ depth `1`, complete ready/retry/DLQ passive telemetry and unavailable-state proof, terminal-transition accounting, secret-bearing allowlisted retry/terminal status diagnostics and storage-boundary normalization, explicit empty availability protobuf mapping, dedicated existing-weekly-minutes and bounded existing-shift gRPC mapping, typed break persistence, break-aware working-demand enforcement, adjacent-segment coalescing and true-overlap rejection, billing and email dispatch routing, validated durable `outbox_id`/`usage_event_id` completion-cache identity and redelivery stability, required background-task supervision and shutdown, durable redelivery idempotency, single-owner execution lease acquisition/expiry/stale-result rejection, non-budgeted busy redelivery, `ACTIVE`/non-`FREE`/nonblank-subscription/future-paid-through claim and result-persistence barriers, exact positive reservation plus debit/refund `balanceAfter` provenance including same-tenant intervening wallet activity, invalid-provenance durable quarantine acknowledgement without handler execution or requeue, verified terminal replay after entitlement loss without DLQ or writes, atomic retry-safe refund settlement, atomic solved-shift/revision/job completion, Tenant-before-advisory-before-Schedule persistence order, overflow rollback, pre-shift/revision entitlement and provenance rejection, active-location/timezone, monotonic draft revision and shift-snapshot guards, deterministic persistence, rollback behavior, and payload limits.

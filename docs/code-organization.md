# Code Organization Hotspots

This is the production-readiness maintainability map for the rebuild checkout. It records source files that are large enough to hide mixed ownership, duplicated policy, or hard-to-test control flow.

Line counts were refreshed on 2026-07-16 at checkpoint `2544f1d` from non-generated `apps/` and `packages/` source files, excluding tests, specs, build output, legacy `old/`, and generated artifacts. The hygiene suite now requires every non-generated source file over 500 lines to appear in this document.
Tests and specs remain outside that mandatory threshold but may be listed when their fixture ownership is itself a material risk.

## Status

- Fixed in this lane: README sibling inventories are covered by `tests/hygiene/repository-hygiene.test.mjs`, including tracked and untracked files. This document adds the missing maintainability routing for oversized source files.
- Fixed in this lane: the production launch validator is now a thin CLI over cohesive public identity, provider, managed-secret, recovery, and shared policy owners. Its focused 33-case suite continues to exercise the fail-closed behavior through the public CLI.
- Deferred in this lane: application-code refactors. This slice owns docs, tests/hygiene, package/turbo config, and code-organization docs, so the source hotspots below are documented for follow-up rather than edited here.

## Public SaaS Hotspots

| File | Lines | Status | Risk | Next extraction |
| --- | ---: | --- | --- | --- |
| `apps/web/app/dashboard/lunch-breaks/page.tsx` | 3335 | Deferred | Page owns date math, session storage, data loading, manual shift setup, planner state, policy editing, break generation, and rendering. This blocks isolated tests for the lunch/break workflow. | Split into `lib/lunch-breaks` pure time/session helpers, data hooks, and smaller panel components before feature expansion. |
| `apps/api/src/admin/admin.controller.ts` | 2259 | Deferred | One controller owns platform stats, tenants, users, audit, credits, plans, health checks, validation, and Prisma transaction policy. Endpoint ownership is too broad for public SaaS admin risk. | Move tenant, user, plan, credit, and health orchestration into dedicated services; keep the controller as transport glue. |
| `apps/web/app/dashboard/scheduling/page.tsx` | 2468 | Deferred | Scheduling page combines API calls, shift window math, staff normalization, modal state, timeline rendering, and optimistic mutations. | Extract schedule API client/hooks and shift date/role helpers; split create/edit/delete UI panels from the route page. |
| `apps/web/app/admin/plans/PlansClient.tsx` | 927 | Deferred | Plan admin UI owns parsing, validation, filtering, create/edit/delete forms, notices, and table rendering in one client component. | Extract plan parsing/normalization helpers and separate list, create form, and edit form components. |
| `apps/api/src/auth/auth.service.ts` | 2895 | Deferred | Auth service owns tenant login resolution, password/PIN auth, session tokens, MFA/TOTP, backup codes, and OAuth code exchange. Security policy changes are hard to review in isolation. | Split credential verification, session issuance, MFA/TOTP, and OAuth adapters into focused services with explicit contracts. |
| `apps/api/src/admin/tenant-account-lifecycle.ts` | 750 | Deferred | Tenant lifecycle helpers now own retention policy serialization, export projection, audit-log purge bypass, and hard-delete dependency ordering. That makes privacy/export and destructive purge behavior too easy to entangle. | Split export projection from retention purge execution, and keep dependency ordering in a small tested purge planner/helper. |
| `apps/api/src/admin/tenant-export.service.ts` | 2048 | Deferred | Durable export ownership now combines job authorization, lease/claim transitions, repeatable-read collection paging, redacted projection, shared artifact streaming, expiry, and cleanup. The state machine is tested, but storage, persistence, and projection policy are concentrated in one service. | Extract the model coverage/projection catalog and artifact-store adapter first, while retaining one narrow service as the transactional job-state owner. |
| `apps/api/src/admin/tenant-cancellation-lifecycle.service.ts` | 1821 | Deferred | Subscription cancellation, provider reconciliation, deletion gating, leases, audit, and recovery policy share one service. | Extract provider transport and pure cancellation-state policy while retaining one transactional lifecycle coordinator. |
| `apps/api/src/admin/tenant-deletion-billing.service.ts` | 1375 | Deferred | Tenant deletion billing reconciliation combines subscription cleanup, credit provenance, refunds, leases, and terminal evidence. | Split credit-settlement verification from provider cancellation and retain one ordered deletion-billing state owner. |
| `apps/web/app/admin/users/AdminUsersWorkspace.tsx` | 1133 | Deferred | Admin user management mixes catalog loading, filtering, role/permission editing, invite flow, mutations, and rendering. | Extract access-catalog hooks, user mutation helpers, and role editor components. |
| `apps/worker/main.py` | 2401 | Deferred | Worker entrypoint owns process setup, RabbitMQ/Postgres connections, metrics server, job dispatch, solve-job status transitions, gRPC client calls, persistence, retry routing, and PDF/billing placeholder handlers. | Extract connectors, job dispatch, schedule-solve lifecycle, and runtime lifecycle into separate modules. |
| `apps/worker/src/billing_usage.py` | 615 | Deferred | Metering persistence, Stripe transport, periodic snapshot preparation, retry claims, and bounded dead-letter recovery share one module. The state transitions remain transactionally explicit and tested, but additional billing operations would make ownership harder to review. | Extract PostgreSQL usage-event persistence into a focused store module before adding another metering state transition; keep Stripe transport and cycle orchestration separate. |
| `apps/worker/src/availability_import_store.py` | 749 | Deferred | Availability-import payload validation, transactional claims, tenant/target/credit locking, completion and retry transitions, terminal cleanup, storage-key resolution, retention sweeping, and database connection policy share one module. | Extract claim/transition persistence into a focused store and move source cleanup plus retention scheduling into separate owners while keeping identity and credit checks transactionally explicit. |
| `apps/worker/src/password_reset_email.py` | 647 | Deferred | Password-reset email configuration, envelope decryption, PostgreSQL outbox leasing and transitions, Resend transport, dispatch, sweeping, metrics, and runtime-loop ownership are concentrated in one module. | Extract the PostgreSQL outbox store and provider adapter first, leaving a narrow dispatcher and a separately owned sweep/runtime loop. |
| `apps/web/app/admin/tenants/TenantsClient.tsx` | 1084 | Deferred | Tenant admin UI combines tenant parsing, lifecycle mutations, billing fields, filters, forms, and table rendering. | Split tenant API/mutation helpers from lifecycle controls and edit/create forms. |
| `apps/web/app/dashboard/settings/SettingsWorkspace.tsx` | 845 | Deferred | Settings workspace owns settings fetch/save, validation, multiple settings sections, notices, and UI composition. | Move settings schema/normalization to a helper and split each settings section into a narrow component. |
| `apps/web/app/dashboard/settings/AccountLifecyclePanel.tsx` | 568 | Deferred | Account lifecycle UI combines status polling, export recovery and downloads, subscription cancellation, deletion receipt transfer, retention display, and destructive-action forms. | Extract export state into a dedicated hook/panel and isolate deletion receipt/session termination from the general lifecycle view. |
| `apps/api/src/settings/settings.controller.ts` | 515 | Deferred | Controller combines normalization, environment-backed OIDC policy, general/team/security mutations, tenant persistence, and immutable security audit projection. The transactional boundary is explicit, but policy parsing and transport ownership are concentrated. | Extract settings normalization and security audit projection into pure helpers, then move mutation orchestration into a focused service while retaining one tenant transaction. |
| `apps/web/components/scheduling/StaffScheduler.tsx` | 908 | Deferred | Scheduler component owns layout measurement, drag/drop, coverage tone math, action dialogs, event rendering, and mobile behavior. | Extract scheduler geometry/drag state and action-dialog components; keep the grid component presentational. |
| `apps/web/app/onboarding/page.tsx` | 1229 | Deferred | Onboarding page mixes step workflow, OTP send/verify/retry, first-location recovery, signup payload assembly, countdowns, form rendering, and route-local CSS. | Move the onboarding state machine and API transitions into a hook, split step and recovery views into components, and move route styling into a dedicated stylesheet without changing the verified recovery contract. |
| `apps/web/app/auth/login/page.tsx` | 794 | Deferred | Login page owns tenant resolution, password/PIN/OTP/MFA branches, timers, redirects, and rendering. | Extract auth-flow state machine and branch-specific form components. |
| `apps/web/app/mfa/page.tsx` | 670 | Deferred | MFA enrollment and challenge UI combines session recovery, TOTP setup, backup-code confirmation, challenge submission, redirects, and rendering. | Extract MFA API/state transitions into a focused hook and split enrollment, challenge, and recovery-code panels. |
| `apps/web/app/dashboard/DashboardWorkspace.tsx` | 958 | Deferred | Dashboard workspace combines navigation state, summary loading, metrics display, and dashboard composition. | Split summary data hook and dashboard cards from shell state. |
| `apps/api/src/lunch-breaks/lunch-breaks.service.ts` | 1950 | Deferred | Service mixes feature access, policy normalization, shift discovery, break scheduling, persistence, and mutation guards. | Extract pure scheduler/policy helpers and persistence guard services. |
| `apps/api/src/availability-imports/availability-imports.service.ts` | 531 | Deferred | Import authorization, encrypted source persistence, credit reservation, lifecycle recovery, cancellation, and response projection share one service. | Extract source-storage and settlement policy helpers while retaining one transaction owner for import state. |
| `apps/api/src/billing/metering.service.ts` | 509 | Deferred | Administrative grants, feature-use debits, replay validation, entitlement checks, and immutable balance settlement share one service. | Separate grant and feature-debit policy behind one canonical ledger settlement primitive. |
| `apps/api/src/common/bootstrap-security.ts` | 527 | Deferred | Startup validation combines proxy, origin, cookie, secret, database-role, and production transport policy. | Split pure configuration validators by security boundary and leave startup orchestration as a small fail-closed coordinator. |
| `apps/api/src/webhooks/webhook-delivery.store.ts` | 730 | Deferred | The store now owns encrypted event persistence, first-attempt claims, publication recovery leases, replay claims, guarded terminal transitions, envelope cryptography, and retry timing. The state machine is tested but too broad for one persistence owner. | Extract envelope encryption and retry/lease policy into pure helpers, then separate event creation from replay transition persistence while retaining one transactional store boundary. |
| `apps/api/src/webhooks/webhook-replay.worker.ts` | 710 | Deferred | The worker combines RabbitMQ topology, replay/recovery coordination, metrics, health HTTP serving, in-flight tracking, graceful signal shutdown, and process startup. These paths are covered, but transport lifecycle and delivery policy are difficult to review independently. | Extract the runtime health/metrics server and signal lifecycle into focused modules, then keep message settlement and recovery orchestration in the worker owner. |
| `apps/engine/src/solver/logic.py` | 1349 | Deferred | Solver logic now combines request normalization, availability windows, skill/demand prechecks, OR-Tools model assembly, solve result serialization, and break staggering. | Split request normalization, infeasibility prechecks, and model construction into focused solver modules before adding more scheduling rules. |
| `apps/web/app/dashboard/staff/StaffWorkspace.tsx` | 961 | Deferred | Staff page combines roster loading, filtering, invite/edit/delete flows, role handling, and rendering. | Extract staff API/mutation hooks and form components. |
| `apps/web/app/admin/credits/CreditsClient.tsx` | 740 | Deferred | Credits admin UI combines parsing, grant workflow, filters, tenant/user display, and transaction rendering. | Extract credit grant form and transaction table components; move parsing to helpers. |
| `apps/web/app/dashboard/layout.tsx` | 334 | Deferred | Dashboard layout owns navigation state, logout flow, role/permission filtering, tenant branding, mobile shell state, and route chrome. | Extract navigation model building and permission filtering into helpers; split desktop and mobile navigation components. |
| `apps/web/app/dashboard/scheduling/print/page.tsx` | 513 | Deferred | Print scheduling owns scope loading, location and date filtering, printable schedule state, stale-request guards, and presentation. | Extract schedule request state into a focused hook and move print presentation into narrow components. |
| `apps/worker/tests/test_worker.py` | 2184 | Deferred | Worker test coverage is valuable but now centralizes AMQP, gRPC, metrics, solve-job lifecycle, retry, and payload contract fixtures in one file. | Split schedule-solve, retry/error handling, and parser/metrics tests into smaller test modules. |
| `apps/engine/tests/test_solver.py` | 939 | Deferred | Solver tests now combine break policy, request validation, availability, skills, demand windows, timezone behavior, infeasibility details, and fairness coverage in one module. | Split demand-window/model tests from break-policy and request-validation tests while retaining the shared solver fixtures. |
| `apps/api/src/billing/stripe.service.ts` | 3201 | Deferred | Credit-pack purchasing is extracted, but the remaining Stripe owner still combines subscription Checkout, customer and portal lifecycle, webhook authentication, tenant resolution, canonical event ordering, subscription synchronization, plan changes, deletion session/subscription cleanup, Customer PII deletion, guarded local binding clear, and billing recovery. The concentration makes destructive billing-lifecycle changes difficult to review independently. | Extract tenant purge cleanup and canonical subscription-state transitions into separate owners first; keep signature verification and transport dispatch in the service facade. |
| `apps/api/src/billing/stripe.service.spec.ts` | 4138 | Deferred | Subscription, portal, deletion/PII cleanup, event-ordering, tenant-ownership, and recovery coverage shares one very large fixture and suite. The breadth protects critical behavior but makes focused failures and fixture ownership difficult to isolate. | Split tenant purge and subscription Checkout tests from canonical webhook synchronization coverage without duplicating shared Stripe fixtures. |
| `apps/api/src/billing/stripe-credit-purchase.service.ts` | 1152 | Deferred | Fixed-pack catalog exposure, paid-subscription Checkout gating, open-Session reuse, authoritative webhook verification, audit persistence, transactional fulfillment, and idempotent non-grant refunds share one service. The payment boundary remains cohesive, but another purchase or compensation flow would overload it. | Keep this owner limited to credit packs; extract pure Session/line-item verification and grant-or-refund disposition policy before adding another payment product. |
| `apps/api/src/billing/stripe-credit-purchase.service.spec.ts` | 1212 | Deferred | Deep fixed-pack, gate, ownership, metadata, line-item, delayed-payment, concurrent replay, refund, and failure coverage is centralized around one service fixture. The scenarios now exceed a comfortable single-suite ownership boundary. | Split catalog/Checkout tests from grant-or-refund webhook tests while retaining one shared Stripe and transaction fixture module. |
| `apps/api/src/billing/stripe-meter-error.service.ts` | 770 | Deferred | Meter recovery now combines Stripe error classification, identifier rotation, ambiguous aggregate reconciliation, retry scheduling, operator replay, and terminal-state handling. The transitions are covered, but billing recovery policy is concentrated in one service. | Extract error classification and aggregate candidate reconciliation into pure policy helpers while retaining a single transactional owner for retry state changes. |
| `apps/api/src/schedules/schedules.controller.ts` | 2110 | Deferred | Schedules controller owns schedule CRUD, publish/coverage/availability validation, date parsing, feature gates, persisted solver inputs, transactional credit reservation and outbox creation, job reads, and status serialization. Rabbit publication is now isolated, but request orchestration and scheduling policy remain too broad. | Extract schedule-window and publish-readiness policy first, then move auto-schedule transaction assembly into a dedicated service while keeping the outbox publisher separate. |
| `apps/api/src/schedules/schedule-solve-outbox.publisher.ts` | 730 | Deferred | Broker publication, lease recovery, terminalization, credit provenance, exact refund settlement, and metrics share one publisher. | Extract terminal settlement and publication-recovery policy into focused owners while preserving one fenced transaction boundary. |
| `apps/api/src/auth/rbac.service.ts` | 2182 | Deferred | RBAC service owns catalog loading, assignment validation, role mutation, permission checks, and default-role behavior. | Split catalog reads, assignment writes, and permission evaluation into focused modules. |
| `apps/api/src/shifts/shifts.controller.ts` | 1197 | Deferred | Controller owns shift CRUD, roster lookup, durable create replay, bulk assignment, overlap checks, date parsing, and draft schedule creation. | Move validation, overlap detection, idempotency persistence, and draft schedule creation into a shift service. |
| `apps/api/src/time-cards/time-cards.controller.ts` | 573 | Deferred | Controller combines paginated team reads, active-card lookup, idempotent clock-in, clock-out, correction workflow delegation, tenant/location/shift validation, permission policy, time parsing, break normalization, and response serialization. | Extract clock-event transaction orchestration and scope validation into focused services, leaving transport, pagination parsing, and serialization in the controller. |
| `apps/api/src/users/users.controller.ts` | 1176 | Deferred | Users controller combines invites, role changes, PIN reset, self PIN updates, deletion, access catalog, role CRUD, and permission validation. | Split user lifecycle, role management, and access-control mutations into services. |
| `apps/api/src/users/staff-invitation-outbox.service.ts` | 575 | Deferred | Invitation creation, encryption, leasing, retry policy, delivery transitions, retention, and metrics share one service. | Extract encrypted payload handling and retry policy while retaining one invitation state-store owner. |
| `packages/db/prisma/schema.prisma` | 1407 | Deferred | Prisma schema now owns tenant core, RBAC, schedules, solve jobs, shifts, breaks, time cards, billing, audit logs, notifications, and webhooks in one file. | Keep domain comments clear and route DB ownership through forward SQL migrations and model-specific docs until a schema split is practical. |
| `apps/api/src/auth/auth.controller.ts` | 892 | Deferred | Auth controller owns origin/CSRF policy, cookie setting, login redirects, OTP, PIN, MFA, refresh, logout, and current-user response. | Extract request security/cookie helpers and keep endpoint methods focused on transport. |
| `apps/web/app/dashboard/lunch-breaks/setup-shifts-recovery.ts` | 520 | Deferred | Browser recovery owns durable request identity, session storage, response-loss replay, concurrency fencing, and retry state. | Extract storage serialization and pure recovery-state transitions behind one small workflow coordinator. |
| `apps/web/app/dashboard/payroll/use-payroll-workspace.ts` | 556 | Deferred | Payroll data loading, mutation recovery, optimistic state, pagination, and notices share one hook. | Split query state from command/recovery state and keep period workflow transitions explicit. |
| `apps/worker/src/staff_invitation_outbox.py` | 641 | Deferred | Worker configuration, database leasing, encrypted payload handling, provider delivery, retry transitions, sweeping, and runtime loop share one module. | Extract the PostgreSQL store and provider adapter, leaving a bounded dispatcher and runtime owner. |
| `apps/worker/tests/test_availability_import.py` | 1256 | Deferred | Availability import parser, persistence, billing, retry, retention, and failure fixtures are concentrated in one test module. | Split parser and source-handling tests from database lifecycle and settlement scenarios. |
| `apps/worker/tests/test_staff_invitation_outbox.py` | 829 | Deferred | Invitation encryption, leases, retries, provider transport, retention, metrics, and runtime tests share one fixture-heavy module. | Split store/state-machine tests from transport and runtime-loop coverage. |

## Infrastructure Hotspots

| File | Lines | Status | Risk | Next extraction |
| --- | ---: | --- | --- | --- |
| `.github/workflows/ci.yml` | 2693 | Deferred | One workflow owns static analysis, Terraform, Semgrep/CodeQL security analysis, dependency audit, unit/integration/E2E tests, seven image builds, release registry state, runtime secret materialization, retained evidence, staging, production deployment, smoke, and rollback. Repeated variable and artifact mappings can drift between release phases. | Extract validation and image publication into reusable workflows, then move production input binding/deploy/rollback into one separately reviewed reusable workflow with a shared configuration contract. |

## Completed Infrastructure Extractions

Line counts for the production launch validator were refreshed on 2026-07-13 after decomposition.

| File | Lines | Status | Ownership | Review boundary |
| --- | ---: | --- | --- | --- |
| `scripts/validate-production-launch.mjs` | 70 | Fixed | Parses CLI input, creates the shared context, sequences policy owners, and renders the single result. | Keep policy logic out of the coordinator and preserve its fail-closed order and CLI contract. |
| `scripts/production-launch-policy-shared.mjs` | 309 | Extracted | Owns env parsing, the error collector, and generic host, URL, required-value, secret, and proof-reference assertions. | Keep helpers policy-neutral; domain-specific launch decisions belong in an owner below. |
| `scripts/production-launch-policy-public-identity.mjs` | 281 | Extracted | Owns public identity, legal approval, signup, transport, browser endpoint, and OIDC policy. | Keep public-facing identity and runtime surface decisions together without provider or secret-file checks. |
| `scripts/production-launch-policy-provider-billing.mjs` | 214 | Extracted | Owns production target, database and RabbitMQ connection identity, email provider, and Stripe launch configuration checks. | Keep provider configuration checks separate from billing implementation and product entitlement logic. |
| `scripts/production-launch-policy-managed-secrets.mjs` | 222 | Extracted | Owns secret strength, managed paths, role isolation, and optional deployment-host file verification. | Keep filesystem and credential-material checks here; callers provide only role and path policy inputs. |
| `scripts/production-launch-policy-backup-recovery.mjs` | 227 | Extracted | Owns off-host backup, PITR, status health, and retained recovery evidence policy. | Keep recovery configuration and proof requirements together without deployment orchestration. |

## Hygiene Rule

The oversized-source threshold is intentionally conservative at 500 lines. A file can exceed it temporarily, but the public SaaS maintainability risk must be visible in this document before the migration hygiene suite passes.

## Recovery Debt

`schedules.controller.ts` and `lunch-breaks.service.ts` are temporarily recovered from the latest successful compiled API artifacts after a local editing-tool truncation. Their focused runtime suites and API typecheck pass, but the compiled-style TypeScript and relaxed class type aliases are maintenance debt. Reconstruct idiomatic typed source from the generated declarations and current behavioral tests before broad feature work in either module.

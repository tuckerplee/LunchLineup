# Security Policy Data

- `README.md`: indexes repository security policy data.
- `codeql-baseline.json`: reviewed, owned, expiring CodeQL findings keyed by CodeQL's line-hash and start-column primary-location fingerprints; new, duplicate, expired, and stale findings fail.

# Assets Directory

Contains all static front-end resources.

- **css/** – Stylesheets used by the application. Shared variables and the Inter font family live in `css/base.css`.
- **js/**  – JavaScript for the interactive schedule interface
- **templates/** – Email templates editable via the admin interface

See the READMEs in each subfolder for details.

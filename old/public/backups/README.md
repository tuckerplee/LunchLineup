# Backups

Encrypted database backups generated by the super admin backup tool are stored here.
Each company gets its own subdirectory named after a slugified company name. Inside
that folder, backups are organized by date and saved using the time as the filename
(`company/date/time.sql.enc`). Files use the `.sql.enc` extension and are AES-256-CBC
encrypted with the password supplied during backup.

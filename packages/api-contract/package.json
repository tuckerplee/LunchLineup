{
  "name": "@lunchlineup/api-contract",
  "version": "0.1.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./src/index.ts",
  "scripts": {
    "generate": "node codegen/generate-client.mjs",
    "build": "npm run generate && tsc",
    "typecheck": "npm run generate && tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@sinclair/typebox": "^0.34.52"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "vitest": "^4.1.8"
  }
}
